# {PROJECT_NAME} — Extension: {FEATURE_NAME}

> Generated by IAAF (Extend Mode) on {DATE}
> Project Stack: {DETECTED_STACK}

---

## Feature Overview

### What it does
{1-2 paragraphs: what the feature adds, who uses it, what problem it solves}

### How it fits
{How this feature integrates with the existing project — which modules it touches, what it reuses}

---

## Data Model Changes

### New Tables
**{table_name}**
| Field | Type | Notes |
|-------|------|-------|
| {field} | {type} | {constraints, relations to existing tables} |

### Modified Tables
| Table | Change | Why |
|-------|--------|-----|
| {existing_table} | {add column / add index / modify column} | {reason} |

### Migration SQL
```sql
{CREATE TABLE and ALTER TABLE statements}
```

---

## New Routes

| Method | Path | Controller | Description |
|--------|------|-----------|-------------|
| {GET/POST/etc.} | {following existing URL pattern} | {Controller@method} | {what it does} |

---

## New Files

| File | Purpose | Pattern followed |
|------|---------|-----------------|
| {path matching existing convention} | {what it does} | {existing file it mirrors} |

---

## Modified Files (minimal)

| File | Change | Why |
|------|--------|-----|
| {existing file} | {what to add/change} | {to connect the new feature} |

---

## Build Steps

### Step 1: {Title}

**Create/Modify:**
- `{file path}` — {what to do}

**Verify:**
{How to confirm this step works}

### Step 2: {Title}
{Continue...}

---

## CLAUDE.md for Build Phase

```markdown
# {Project Name} — Extension: {Feature}

{One-line description of what's being added}

## Commands

{Existing project commands}

## What Exists

{Brief description of current project state — stack, architecture, key files}

## What We're Adding

{Brief description of the feature}

## Build Steps

Follow these steps in order. Verify each before proceeding.

### Step 1: {title}
{instructions}
**Verify:** {criteria}

### Step 2: {title}
{instructions}
**Verify:** {criteria}

## The Foreman

- Read existing files before creating new ones. Understand the patterns first.
- New files MUST follow existing naming and organization conventions.
- Prefer editing existing files minimally — add what's needed, don't restructure.
- Do not re-read files already in context.
- Verify each step before moving forward.
- Do not add features beyond what the plan specifies.
- Match existing code style exactly (indentation, naming, patterns).
- User instructions always override this file.

## Non-Negotiable Rules

1. New code follows existing project conventions.
2. Run existing tests after each step — do not break what works.
3. New migrations are additive — never modify migrations that have already run.
4. New routes follow the existing URL pattern.
5. {Project-specific rule}
```
