# {PROJECT_NAME} — Improvement Plan

> Generated by IAAF (Improve Mode) on {DATE}
> Project Stack: {DETECTED_STACK}

---

## Current State

### Stack
{Detected technologies with versions}

### Architecture
{Pattern: MVC, monolith, etc. How components are organized.}

### Scale
{Number of files, models, routes, tests}

---

## Issues Found

| # | Issue | Severity | Effort | Impact | Files |
|---|-------|----------|--------|--------|-------|
| {n} | {description} | {High/Medium/Low} | {Low/Medium/High} | {Low/Medium/High} | {file paths} |

---

## Improvement Steps

{Ordered by dependency and priority. Each step is self-contained.}

### Step 1: {Title}

**Issue:** {What's wrong and why it matters}
**Files to modify:**
- `{path/to/file.ext}` — {what to change in this file}

**What to do:**
{Specific, concrete instructions. Not vague — exact changes.}

**Verify:**
{How to confirm the improvement works. Test command, curl, visual check, etc.}

---

### Step 2: {Title}

{Same format...}

---

## CLAUDE.md for Build Phase

```markdown
# {Project Name}

{One-line description}

## Commands

{Existing project commands — detected from package.json, composer.json, Makefile, etc.}

## Tech Stack

{Detected stack}

## Current Architecture

{Brief description of how the project is organized}

## Improvement Plan

Follow these steps in order. Each step has verification criteria — do not proceed until verified.

### Step 1: {title}
{instructions}
**Verify:** {criteria}

### Step 2: {title}
{instructions}
**Verify:** {criteria}

{Continue for all approved steps}

## The Foreman

- Be concise. No sycophantic openers or closing fluff.
- Read the files referenced in each step BEFORE modifying them.
- Prefer editing existing files over rewriting.
- Do not re-read files already in context.
- Verify each step before moving to the next.
- Do not refactor code beyond what the step requires.
- Do not change patterns, naming conventions, or architecture unless the step explicitly says to.
- Respect the existing codebase style. Match indentation, naming, and patterns.
- User instructions always override this file.

## Tool Call Awareness

- Read all files for a step at once, not one at a time.
- Make all changes for a step in one pass when possible.
- If a step takes more than 15 tool calls, reassess.

## Non-Negotiable Rules

1. {Project-specific rule — e.g., "All PHP files follow PSR-12"}
2. {Project-specific rule — e.g., "Never modify migration files that have already run"}
3. {Project-specific rule — e.g., "All API responses use the existing response helper"}
4. Match the existing code style. Do not introduce new patterns.
5. Run existing tests after each step. Do not break what works.
```
